export interface Cv {
    id: number;         // Unique identifier for the CV
    name: string;       // Last name of the individual
    firstname: string;  // First name of the individual
    age: number;        // Age of the individual
    cin: string;        // CIN (National Identification Number)
    job: string;        // Job title
    path: string;       // Path to the image (profile picture)
  }
  